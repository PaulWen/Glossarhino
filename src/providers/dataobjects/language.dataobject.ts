export abstract class LanguageDataobject {

  abstract get languageId(): number;
  abstract set languageId(languageId: number);

  abstract get languageName(): string;
  abstract set languageName(languageName: string);

}
